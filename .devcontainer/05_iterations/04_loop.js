const myObject = {
    js : "javascreipt",
    cpp : "c++",
    rb : "reby",
    swift : "swift"
}

for (const key in myObject) {
    // console.log(`${key} shortcut is for ${myObject[key]} `);
    
}

const myArray = ["js", "cpp", "rb", "py"]

for (const key in myArray) {
    
    console.log(`${key} shortcut is for ${myArray[key]} `);
        
    }

    // map not iterable